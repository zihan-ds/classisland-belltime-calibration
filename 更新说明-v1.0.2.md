# 铃声自动校时 v1.0.2 · 更新说明

> 本文件随迁移包分发，集中说明 **v1.0.2 这一个版本带来的全部功能更新**、对应设置项、文件与验证方式。
> 架构与长期设计说明见 `README.md`、`plugin/README.md`；开发过程记录见 `docs/HANDOFF.md`。
> **版本号：1.0.2**（`plugin/manifest.yml` 的 `version: 1.0.2.0`）。

---

## 一、本次更新一览

| # | 更新 | 类型 | 谁在用 |
|---|---|---|---|
| 1 | 起响沿闸门成为唯一主判据，删除启发式选铃 | 判定算法 | 全自动 |
| 2 | 偏移估计改为「近期样本中位数」，趋势外推受三门槛约束 | 估计策略 | 全自动 |
| 3 | 偏移样本按天落盘，进程重启恢复当天序列 | 数据持久化 | 全自动 + 离线分析 |
| 4 | 大误差人工复核提醒 | 用户提醒 | 用户（弹窗） |
| 5 | 「人工审核提醒」开关 | 插件设置 | 用户（可关） |
| 6 | **手动拟合**按钮：当天有效样本 → 直接写入偏移 | 手动操作 | 用户（按钮） |
| 7 | 浅色模式可读性修复 | 界面 | 用户（界面） |
| 8 | 并发安全：偏移估计器样本读写加锁 | 内部健壮性 | 全自动 |

---

## 二、逐项说明

### 1. 判定算法：只剩两条带真伪判据的路径，不再回退启发式

- **起响沿闸门（主判据）**：在「课表边界 ±2 s」内找**起响比值**最大处，
  比值 =「起响后 0.5 s 峰值 RMS ÷ 起响前 0.3 s 基线 RMS」。
  真铃声是突然起响（实测 4~346×），环境噪声是渐强的（伪匹配仅 1~3×）；
  ±2 s 时间门同时排除每个边界附近提前 6~15 s 的那次更强起响。
- **模板匹配（辅助）**：NCC 粗定位 → 4 kHz 精定位 → 音色确认 → 攻击沿校验，四道全过才采用。
  样本由使用者自行录制导入（`plugin/Templates/` 内随包的是**静音占位**，插件会识别并跳过）。
- **不再回退「最响持续段」式启发式选铃**：实测其误差可达 −7.8~+8.1 s（选错事件），
  属已确认不准的办法。两条路径都不中即判「无可信测量」并跳过本次——**宁可不校准，也不用脏值**。

### 2. 偏移估计：中位数，而不是趋势外推

- 写值 = **近期样本的中位数**（默认 6 小时窗口；样本不足 3 个则放宽到全部样本）。
  中位数对单次测量的抖动天然稳健，且「所需偏移」只取决于铃的真实滞后、与当前偏移无关，
  因此从任何起点都会收敛到同一值。
- **趋势外推默认关闭**，仅当「样本 ≥12 个、时间跨度 ≥6 小时、拟合残差 <0.3 s」三项同时满足才启用
  （即噪声已被平均到能看出真实走时之后，才允许追趋势），且斜率钳制在 ±2 秒/小时、外推不超过 2 小时。
- **死区门控**：写值与当前偏移之差小于死区（默认 0.3 s）时不动作。
- **大误差复核**：一次要改的偏移超过阈值（默认 ±3 s）时**拒绝写入**（并触发第 4 条提醒）。

### 3. 偏移样本按天落盘（`Logs/offset-samples.jsonl`）

每个拿到**可信测量**的窗口追加一行：

```json
{"Ts":"2026-09-17 20:00:06.952","RequiredSec":-7.1759,"CurrentSec":-7.5120,"Kind":"下课","B":"20:00:00.000","Applied":true}
```

| 字段 | 含义 |
|---|---|
| `Ts` | 样本时刻（本地墙钟，取铃响起响点） |
| `RequiredSec` | 本次测得的「让误差归零所需绝对偏移」（秒） |
| `CurrentSec` | 测量时的当前偏移（秒） |
| `Kind` / `B` | 边界类型与课表显示时刻 |
| `Applied` | 本次是否**真的写入**了偏移——手动拟合据此筛「有效样本」 |

用途：① 进程重启后只载入**当天**样本恢复估计器（避免重启后前几个边界退回「单次测量直接写入」，
日志会写 `偏移样本恢复：载入当天样本 N 个（跳过其它日期 M 个）`）；② 离线画走势、复算估计策略。

### 4. 大误差人工复核提醒

一次要写入的偏移与当前偏移相差超过**大误差复核阈值**（默认 ±3 s）时：插件**拒绝写入**，
并在 ClassIsland 界面上弹出提醒，写明边界时刻、当前偏移、本次测得的所需偏移与差值。

- 只在**真的拒写**时出现；**没有时间冷却**——同一个大误差每次都会提醒；
- 文案与判定：`Services/BellReviewDecision.cs`（纯函数）+ `Services/BellReviewNotifier.cs`（发送）；
- 设置页有**「发送测试提醒」**按钮，可随时确认提醒能正常弹出（该测试不受开关约束）；
- 出现在宿主提醒通道里，可在**设置 → 提醒 → 铃声校时复核**单独关闭。

> 为什么不能只写日志：被静默拦下的偏差，用户根本不知道要去看日志。实机 2026-09-17 出现过
> 5.8 s 量级的偏差被静默拦下、用户毫不知情；而大误差恰恰是最需要人介入的情形
> （校铃钟被人工校过、系统时钟跳变、信号源切换）。

**技术说明（排障时重要）**：提醒没有走「继承公开基类 `NotificationProviderBase`」那条常规插件路径——
在本机内核 2.1.0.1 上实测该路径**静默失效**（派生类构造函数确实以 `autoRegister = true` 调进了基类，
基类也正确填好了名称与 GUID，却始终走不到 `INotificationHostService.RegisterNotificationProvider`，
宿主日志里没有任何注册记录）。现在的做法是：用公开接口方法把自己登记进宿主提醒提供方表
（使其出现在「设置 → 提醒」并可单独开关）+ 反射调用宿主 internal 的 `ShowNotification`
派发提醒。与写时间偏移一样，**只用宿主已有的公开/可反射成员，不改内核、不加补丁**。

### 5. 「人工审核提醒」开关（插件设置，默认开）

关掉后大误差**仍然拒绝写入**，但不再弹提醒、只记日志。
拒写是数据安全底线，提醒只是可选的打扰，因此两者分开控制。
关掉后没有冷却机制，每个边界的大误差都只留一条日志。

### 6. 手动拟合（设置页「手动拟合并应用」）

按下后：读当天已落盘样本 → **只保留有效样本** → 用与运行时**完全相同**的估计器重算 →
**立即写入「应用设置 → 时钟 → 时间偏移」**，**无视学习模式与自动应用开关**（显式手动动作，按下即写）。

- **有效样本 = `Applied = true`**（真正写入过偏移的干净测量）。
  排除 `Applied = false` 是刻意的：它们有两种来源，都不是干净测量——死区带内（偏移没动的存量，
  不代表当前基准）与大误差拒写（含未观测到切换的退化绝对口径）。实机 2026-09-17 的当天 15 条样本里，
  被排除的两条是 −0.91 与 +0.29，与当天真实基准（−7 s 上下）相差近 7 秒。
  这里**只用样本自带的标记**，不另造 MAD/残差之类的启发式阈值。
- 写入后会把当天样本重新装进运行时估计器，避免下一个边界用旧序列把手动结果顶回去。
- 结果文字写在按钮旁（写入前后值、有效样本数、中位数、散度、趋势是否启用），日志同样留痕：
  `手动拟合并应用（设置页按钮）：已写入 -7.176 秒（由 -7.512 改为 -7.176…）；有效样本 6/15；…`
- 离线复算：`ReplayTool manual-fit <offset-samples.jsonl> [日期]`。

### 7. 浅色模式可读性修复

**问题**：设置页原先把颜色硬编码成半透明白——卡片底色 `#18FFFFFF`、提示文字 `#88FFFFFF` / `#AAFFFFFF`。
这套配色只在深色模式下可读，切到浅色模式后**提示文字几乎看不见**（白字压浅色背景）。

**修复**：改为 **ThemeVariant 键控的资源字典**，Avalonia 按当前主题变体自动取对应一组：

| 资源键 | 深色 | 浅色 |
|---|---|---|
| `BellCardBackgroundBrush`（卡片底色） | `#18FFFFFF` | `#0A000000` |
| `BellHintBrush`（提示文字） | `#AAFFFFFF` | `#C0000000` |

- 不依赖宿主是否提供某个具名画刷（不用 `{DynamicResource CardBackgroundFillColorDefaultBrush}`
  这类外部键），插件自己的两套颜色自包含，宿主换主题也不受影响；
- 设置页是缓存实例，主题切换后已实例化控件不一定重跑资源查找，故订阅
  `ActualThemeVariantChanged` 清掉 `bellcard` / `bellhint` 两个语义类上的本地值，让资源重新求值
  （内核自身的 `LessonControlExpanded` 也是手动刷新处理同类问题）；
- 顺带清掉 UI 文案里误留的 Markdown 粗体标记（`**…**` 在界面上是字面星号）。
- **自检**：设置页每次加载/主题切换都会写一行日志，可直接判断资源是否解析成功——
  正常：`设置页主题自检（设置页首次加载）：主题=Light，卡片底色=#0a000000，提示文字=#c0000000 → 主题资源解析正常。`
  异常：`…未取到主题画刷（回退到默认前景色/透明底），浅色模式可读性可能受影响。`

### 8. 并发安全：偏移估计器加锁

手动拟合在 UI 线程上装载新样本序列，而校准在捕获线程上追加样本——两者可能同时碰到同一份序列。
`DriftFitter` 的所有公开成员与内部重算现已由一把内部锁串行化，
「边采集边点手动拟合」不会读出半更新的中间态（那正是脏值来源）。

---

## 三、设置项对照表（v1.0.2 全量）

| 设置 | 默认 | 说明 |
|---|---|---|
| 启用铃声自动校时 | 开 | 总开关 |
| 学习模式 | 开 | 只记录不写；确认稳定后关闭 |
| 自动应用校准偏移 | 关 | 关闭学习模式后才生效 |
| 布防提前秒数 | 20 | 距边界多少秒开始布防 |
| 边界后监听秒数 | 20 | 越过边界后继续监听多少秒 |
| 响铃搜索半窗（±秒） | 12 | 覆盖校铃钟漂移幅度 |
| 应用死区（秒） | 0.3 | 差值小于此值不写入 |
| 忽略的边界时刻 | 08:00,18:30 | 这些边界不监听、不校准 |
| **大误差复核阈值（秒）** | **3.0** | **超过即拒写；同时是「是否提醒」的判据（1~60 s）** |
| **人工审核提醒** | **开** | **关掉后大误差仍拒写，但不弹提醒** |
| 识别灵敏度 | 2.0 | 占位参数 |
| 启用模板匹配 | 开 | 辅助判据开关 |
| 模板目录 / 样本路径 / 截取区间 | — | 见 `README.md` 的「提供你自己的铃声样本」 |
| 匹配阈值：波形 NCC / 音色相似度 | 0.60 / 0.45 | 模板匹配两道阈值 |
| 调试音频转存 | 关 | 开启后窗口音频写 `Dumps/`（默认绝不在磁盘留音频） |

---

## 四、本次更新的文件

新增：

```
plugin/Services/BellReviewDecision.cs    大误差复核提醒的阈值/开关判定（纯函数，可离线断言）
plugin/Services/BellReviewNotifier.cs    复核提醒：文案 + 宿主提醒通道（HostNotificationSender）
plugin/Services/ManualFitService.cs      手动拟合：当天有效样本 → 所需偏移（纯逻辑）
本文件（更新说明）
```

修改：

```
plugin/Services/CalibrationRunner.cs        阈值改读设置项 + 大误差时触发提醒 + ReloadTodaySamples
plugin/Services/DriftFitter.cs              样本读写加锁（并发安全）
plugin/Services/OffsetSampleStore.cs        样本记录带上 Applied 字段（有效样本判据）
plugin/Models/BellCalibrationSettings.cs    新增 LargeErrorLimitSeconds、EnableReviewNotification
plugin/Settings/BellCalibrationSettingsPage.axaml(.cs)
                                            主题资源字典、语义类、主题自检、手动拟合卡片、测试提醒按钮
plugin/Plugin.cs                            提醒通道与写入通道的就绪接线
plugin/tools/ReplayTool/Program.cs(.csproj) notify-test / manual-fit 两个离线断言子命令
```

未改动：判定链路（`OnsetGate.cs`、`RingDetector.cs`、`TemplateMatcher.cs`、`RingCandidateSelector.cs`、
`CaptureAudio.cs`、`MicCaptureService.cs`、`CalibrationScheduler.cs`、`CorrectionPolicy.cs`）、
`manifest.yml`（**版本号未变**）、`plugin/Templates/*.wav`（仍是静音占位）。

---

## 五、如何验证本次更新

```powershell
# 1. 构建（0 warning / 0 error）
dotnet build plugin/BellTimeCalibration.csproj -c Release
dotnet build plugin/tools/ReplayTool/ReplayTool.csproj -c Release

# 2. 离线断言：提醒开关与阈值判定（含「无冷却」回归）
ReplayTool notify-test                        # 全部通过才返回 0

# 3. 离线复算手动拟合（用实机样本文件，会打印有效/被排除样本与拟合值）
ReplayTool manual-fit <offset-samples.jsonl> [yyyy-MM-dd]
```

实机侧：

| 验证点 | 期望 |
|---|---|
| 插件日志启动行 | `大误差人工复核提醒已接线（…当前 开）`、`手动拟合通道就绪（Applier 可用=True）` |
| 宿主日志 | `注册提醒提供方：0b7e1c4a-…（铃声校时复核）` |
| 设置页「发送测试提醒」 | 界面弹出测试提醒 |
| 设置页「手动拟合并应用」 | 结果文字出现；日志 `手动拟合并应用（设置页按钮）：已写入 …`；「时钟 → 时间偏移」同步变化 |
| 设置页主题自检 | `设置页主题自检（…）：主题=Light/Dark，… → 主题资源解析正常。` |
| 浅色模式 | 切换主题后提示文字清晰可读（深色下白字、浅色下深色字） |
